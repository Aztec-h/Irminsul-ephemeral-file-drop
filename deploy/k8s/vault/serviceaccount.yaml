apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault
  namespace: irminsul
  # When switching to IRSA/pod-identity, annotate with the IAM role ARN here.
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/irminsul-dev-vault
automountServiceAccountToken: false
